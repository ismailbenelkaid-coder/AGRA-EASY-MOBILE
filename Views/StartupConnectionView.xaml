<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AGRA_EASY_MOBILE.StartupConnectionView"
             BackgroundColor="White">

    <Grid>
        <Image Source="drop_network_startup_full.png"
               Aspect="AspectFill"
               HorizontalOptions="Fill"
               VerticalOptions="Fill"
               InputTransparent="True" />

        <BoxView BackgroundColor="#55FFFFFF"
                 HorizontalOptions="Fill"
                 VerticalOptions="Fill" />

        <Border BackgroundColor="#EFFFFFFF"
                Stroke="#E2E8F0"
                Padding="18,14"
                Margin="24"
                VerticalOptions="End"
                HorizontalOptions="Fill">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="22" />
            </Border.StrokeShape>

            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                <ActivityIndicator x:Name="StartupIndicator"
                                   IsRunning="True"
                                   IsVisible="True"
                                   WidthRequest="34"
                                   HeightRequest="34"
                                   Color="#16A34A"
                                   VerticalOptions="Center" />

                <VerticalStackLayout Grid.Column="1"
                                     Spacing="2"
                                     VerticalOptions="Center">
                    <Label Text="AGRA EASY MOBILE"
                           FontSize="17"
                           FontAttributes="Bold"
                           TextColor="#0F172A" />

                    <Label x:Name="StartupMessage"
                           Text="Connexion en cours..."
                           FontSize="13"
                           TextColor="#64748B" />
                </VerticalStackLayout>
            </Grid>
        </Border>
    </Grid>
</ContentPage>
